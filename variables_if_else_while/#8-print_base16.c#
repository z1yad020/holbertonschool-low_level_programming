#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * void: nothing
 *
 *
 * Description: if else condition
 *
 *
 *
 * Return: 0
 */
int main(void)
{

	char i, j;

	for (i = 'a', j = ; i <= 'z'; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
