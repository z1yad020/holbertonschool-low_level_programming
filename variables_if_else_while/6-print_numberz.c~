#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - entry point
 * void: nothing
 *
 *
 * Description: if else condition
 *
 *
 *
 * Return: 0
 */
int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		putchar(i);
	}
	putchar('\n');

	return (0);
}
