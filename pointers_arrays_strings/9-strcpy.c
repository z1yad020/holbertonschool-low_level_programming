#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _strcpy - strcpy
 * @dest: destination
 * @src: source
 *
 * Return: pointer char
 */

char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
