#include <stdio.h>
/**
 * main - Makes the sum of two numbers
 * void: First operand
 *
 *
 * Description: This is a longer description.
 * Don't forget that a line should not exceed 80 characters.
 * But you're totally free to use several lines to properly
 * describe your function
 * Return: The sum of the two parameters
 */
int main(void)
{
	printf("Size of a char: %i bytes(s)", sizeof(char));
	printf("Size of an int: %i bytes(s)", sizeof(int));
	printf("Size of a long int: %i bytes(s)", sizeof(long int));
	printf("Size of a long long int: %i bytes(s)", sizeof(long long int));
	printf("Size of a float: %i bytes(s)", sizeof(float));
	return (0);
}
