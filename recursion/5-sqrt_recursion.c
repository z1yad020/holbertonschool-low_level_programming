#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _sqrt_recursion - square root
 * @n: number
 *
 * Return: root
 */

int _sqrt_recursion(int n)
{
	if (y < 0)
		return (-1);

	if (!y)
		return (1);

	return (_pow_recursion(x, y - 1) * x);
}
