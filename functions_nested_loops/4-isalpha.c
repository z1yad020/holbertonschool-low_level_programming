#include "main.h"
#include <ctype.h>
/**
 * _isalpha - printing if is lower case
 * @c: character
 *
 * Descriptio: doing some code
 *
 * Return: void
 */
int _isalpha(int c)
{
	if (isalpha(c))
		return (1);
	return (0);
}
