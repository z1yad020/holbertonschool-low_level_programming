#include "main.h"
#include <ctype.h>
/**
 * _islower - printing if is lower case
 *
 * Descriptio: doing some code
 *
 * Return: void
 */
int _islower(int c)
{
	if (!islower(c))
		return 0;
	return 1;
}
